{
  "name": "project",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "generate": "nuxt generate",
    "predeploy": "npm run generate",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "nuxt": "^3.20.2",
    "vue": "^3.5.25",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}